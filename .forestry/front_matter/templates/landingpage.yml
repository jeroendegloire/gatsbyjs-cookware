---
label: Landingpage
hide_body: false
fields:
- name: landingpage__field_group_key_figures
  type: field_group
  config: {}
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
  - name: landingpage__key_figures
    type: blocks
    label: Key figures
    template_types:
    - key-figure
    config:
      min: 4
      max: 12
  label: Key figures
- name: sortable_list
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: sortable list
- name: test
  type: blocks
  label: test
  template_types:
  - office
  - brand
  config:
    min: 
    max: 
